import React, { Component } from 'react';
import './Header.css';

export class Header extends Component {
  render() {
    return (
      <header>
        <h1>Todo App</h1>
      </header>
    );
  }
}

export default Header;
